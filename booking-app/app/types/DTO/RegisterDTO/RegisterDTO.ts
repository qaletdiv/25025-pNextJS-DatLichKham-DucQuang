export type RegisterDTO = {
    username: string, 
    email: string, 
    password: string
}

export type RegisterForm = {
    username: string, 
    email: string, 
    password: string
    confirmedPassword: string
}